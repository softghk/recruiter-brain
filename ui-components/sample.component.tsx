import React from "react"

const ProfileDataComponent = ({ rating, explanation }) => (
  <div
    style={
      {
        /* your styles here */
      }
    }>
    hello
    <div>Rating: {rating}</div>x<div>{explanation}</div>
  </div>
)

export default ProfileDataComponent
